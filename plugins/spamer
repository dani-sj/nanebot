local function run(msg)
if msg.text == "zetaspammer" then
 return "".. [[ 































   ]]
  end
end

return {
  description = "zeta Spammer", 
  usage = "zetaspammer : send 25000pm for spaming",
  patterns = {
    "^zetaspammer$"
  }, 
 run = run,
    privileged = true,
 pre_process = pre_process
}
